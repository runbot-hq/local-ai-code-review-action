{
  "name": "local-ai-code-review-action",
  "version": "1.0.0",
  "description": "GitHub Action that reviews PRs using a local Ollama model on a self-hosted runner",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts --license licenses.txt && ncc build src/post.ts -o dist-post --license licenses.txt && mv dist-post/index.js dist/post.js && rm -rf dist-post",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.0"
  }
}
